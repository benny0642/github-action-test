on:
  # push:
  #   branches:
  #     - hotfix/**
  pull_request:
    branches:
      - master
    types: [closed]
jobs:
  test-github-context:
    runs-on: ubuntu-latest
    steps:
      - run: echo '${{github.ref}}'
      - run: echo '${{github.head_ref}}'
    